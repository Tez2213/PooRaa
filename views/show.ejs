<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Post Details</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>

  <body>
    <form action="http://localhost:8080/posts">
      <button type="submit" class="btn btn-outline-danger">Home</button>
    </form>
    <br /><br />
    <div class="container mt-5">
      <div class="d-flex flex-column align-items-center p-4 border rounded">
        <img
          src="<%= post.img %>"
          class="rounded-circle mb-3"
          alt="User Image"
          width="150"
          height="150"
        />
        <h5 class="mb-1">
          Username: <span id="username"><%= post.username %></span>
        </h5>
        <p class="mb-1"><strong>Content:</strong> <%= post.content %></p>
        <p class="mb-0">
          <strong>ID:</strong> <span id="userId"><%= post.id %></span>
        </p>
      </div>
    </div>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
